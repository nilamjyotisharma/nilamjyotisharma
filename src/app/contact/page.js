import BaseLayout from "@/layouts/BaseLayout"

export const metadata = {
  title : 'Contact'
}

const page = () => {
  return (
    <BaseLayout>
    <div>Contact us</div>
    </BaseLayout>
  )
}

export default page